import { expect } from "chai";
import { ethers } from "hardhat";

describe("Minnow", function () {
  it("Should do something...", async function () {
    // ...

    expect(1 + 1).to.equal(2);
  });
});
